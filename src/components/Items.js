import React from 'react';
import { useDispatch } from 'react-redux';

function Item() {
    const dispatch = useDispatch();

    return(
        <div>

        </div>
    );
};

export default Item;
